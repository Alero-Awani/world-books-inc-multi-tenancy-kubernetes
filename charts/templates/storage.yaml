
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "helm-chart.appName" . }}-pvc
spec:
  storageClassName: gp2
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi